# 2026-04-10 TK-009 Rework 02

## Rework Status

- Ticket: `TK-009`
- Assignee: `agent-001`
- Requested By: `agent-x`
- Status: `rework required`

## Rework Reason

The backend permission boundary is now closed, but frontend add guidance is still incomplete.

The existing provider/API-Key button on model cards is still directly reachable by ordinary users.

Relevant file:

- `web/src/pages/user-setting/setting-model/components/modal-card.tsx`

## Required Rework Scope

### 1. Guard the provider/API-Key entry on existing model cards

Required outcome:

- ordinary users should not enter the provider/API-Key configuration modal from this path
- ordinary users should immediately receive the same add-side guidance:
  - contact superuser

### 2. Keep behavior aligned with the already-fixed add paths

This path should behave consistently with the add handling already present in:

- available-model add entry
- other ordinary-user add restrictions

### 3. Expand self-test coverage

Must explicitly include:

- ordinary-user click on existing provider/API-Key entry
- expected guidance message visibility
- no modal-entry path for ordinary users from that button

## Out of Scope

- broader model-management redesign
- permission redesign for edit/enable/disable
- backend permission refactor

## Expected Delivery

- frontend add guidance fully consistent across model add/configuration entry points
- no silent or misleading ordinary-user path through existing provider/API-Key button
