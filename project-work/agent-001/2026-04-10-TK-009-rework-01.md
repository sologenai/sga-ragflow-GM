# 2026-04-10 TK-009 Rework 01

## Rework Status

- Ticket: `TK-009`
- Assignee: `agent-001`
- Requested By: `agent-x`
- Status: `rework required`

## Rework Reason

The current implementation did not fully close the backend add-model permission boundary.

`/set_api_key` is still open to ordinary users, while this path still writes provider/model configuration.

Relevant file:

- `api/apps/llm_app.py`

## Required Rework Scope

### 1. Treat `set_api_key` consistently with the add restriction

If `set_api_key` remains a provider/model configuration write path, ordinary users must not be allowed to use it successfully.

Required outcome:

- ordinary users cannot use `/set_api_key` successfully
- superusers can still use `/set_api_key` normally

### 2. Keep frontend add guidance aligned

Frontend add guidance already exists. Keep it aligned with the backend restriction so product behavior remains coherent.

### 3. Expand self-test coverage

Must explicitly include:

- superuser provider/API-key config path
- ordinary-user provider/API-key config path

## Out of Scope

- broader model permission redesign
- edit/enable/disable redesign
- global LLM ownership redesign

## Expected Delivery

- backend add path fully restricted to superuser
- provider/API-key path no longer bypasses the intended permission boundary
- updated completion record with the added test coverage
