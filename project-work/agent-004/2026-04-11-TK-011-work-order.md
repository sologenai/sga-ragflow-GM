# 2026-04-11 TK-011 Work Order

## Basic Information

- Ticket ID: `TK-011`
- Title: Full-surface Chinese UI i18n audit and remediation
- Assignee: `agent-004`
- Dispatcher: `agent-x`
- Status: `pending acceptance`
- Priority: `high`
- Type: `special-remediation`

## Background

The current build still contains many Chinese-locale UI defects, including:

- raw i18n keys rendered in the interface
- garbled Chinese text
- untranslated English labels in Chinese flows
- missing localized labels in dropdowns, modals, and tooltips

This is confirmed to occur in dataset settings and is treated as a system-wide audit issue, not a one-page defect.

Reference document:

- `project-work/agent-x/2026-04-11-ui-i18n-full-audit-assessment.md`

## Goal

Audit and repair Chinese UI display problems across the whole product surface.

This ticket is not complete unless the assignee checks every reachable page and the associated dropdowns/modals/tooltips.

## Required Outcomes

### 1. Full UI audit coverage

The assignee must inspect all reachable surfaces in the current product, including:

1. main navigation pages
2. admin pages
3. user settings pages
4. knowledgebase pages
5. chat-related pages
6. agent-related pages
7. search pages
8. memory pages
9. file-management pages
10. every dropdown menu
11. every modal/dialog
12. important tooltip/help-text areas

### 2. Fix visible Chinese-locale defects

The remediation must cover at least these defect classes:

- raw i18n keys rendered directly
- garbled Chinese / mojibake
- missing Chinese fallback labels
- broken locale mappings in settings forms
- untranslated action labels in Chinese mode

### 3. Validate in browser, not only by code search

This ticket requires real UI traversal.

Code search can assist discovery, but acceptance depends on actual browser-based validation of the screens and controls.

### 4. Keep scope focused

This ticket is about visible i18n/UI wording defects.

Do not expand it into:

- whole design refresh
- layout redesign
- unrelated feature work
- backend functional refactors unless strictly required to expose correct localized text

## Suggested Files To Inspect

- `web/src/locales/zh.ts`
- `web/src/locales/en.ts`
- `web/src/routes.tsx`
- `web/src/pages/**`
- `web/src/components/**`

## Execution Requirements

1. Use browser-based traversal to inspect every page and control surface.
2. Fix the visible issues in code.
3. Keep a page-by-page checklist in the completion record.
4. If a page cannot be reached because of a separate functional defect, record it explicitly instead of skipping it silently.

## Acceptance Focus

`agent-x` will primarily verify:

1. whether the assignee actually performed full-surface inspection
2. whether dropdown/menu/modal/tooltip defects were included
3. whether Chinese-locale raw keys/garbled strings are materially reduced across the product
4. whether the completion record contains an explicit coverage checklist

## Self-Test Requirements

Before handoff, at minimum verify and report:

1. main pages inspected: pass / fail
2. admin pages inspected: pass / fail
3. settings pages inspected: pass / fail
4. dropdown menus inspected: pass / fail
5. modal dialogs inspected: pass / fail
6. tooltip/help text inspected: pass / fail
7. raw-key rendering issues fixed in discovered pages: pass / fail
8. garbled Chinese issues fixed in discovered pages: pass / fail

## Required Documentation Update

After execution, update:

1. this work-order status to:
   - `pending acceptance`
   - or `blocked`

2. add completion record:
   - `project-work/agent-004/2026-04-11-TK-011-completion.md`

The completion record must include:

- ticket id
- completion time
- actual changed files
- implementation summary
- self-test results
- page-by-page coverage checklist
- known limitations
