# 2026-04-04 TK-003 Rework 01

## Rework Basis

- Source ticket: `TK-003`
- Rework round: `01`
- Requested by: `agent-x`
- Date: `2026-04-04`
- Reference: `project-work/agent-x/2026-04-04-TK-003-review-round-1.md`

## Required Items

1. 修复前端字符串语法错误（`dataset-source.ts`、`index.tsx`）。
2. 后端对 `kb_label` 增加固定值校验（create/update）。
3. 回归手工补标与筛选链路。

## Rework Result

- Rework status: `done`
- Completion time: `2026-04-04 22:20:58 +08:00`

### Item 1: Frontend syntax repair

- 已修复 `未标注` 文案字符串，源码可正常解析。
- 文件：
  - `web/src/pages/datasets/dataset-source.ts`
  - `web/src/pages/datasets/index.tsx`

### Item 2: Backend fixed-value enforcement

- `KnowledgebaseService.create_with_name` 已限制 `kb_label` 固定值。
- `kb_app.update` 已限制 `kb_label` 固定值并归一化。
- `KnowledgebaseService.update_by_id` 新增统一兜底校验，阻止其他更新调用旁路写入非法值。
- 允许值：
  - `manual`
  - `chat_graph`
  - `news_sync`
  - `archive_sync`
  - `""`（未标注）

### Item 3: Chain re-check

- 手工补标入口保留在知识库卡片菜单中。
- 卡片标签显示链路保持可用。
- 列表标签筛选链路保持可用。
- 与搜索和 owner 过滤叠加逻辑保持兼容（先服务端过滤，再本地标签筛选）。

## Verification Notes

- `python -m py_compile`：通过（`kb_app.py`、`knowledgebase_service.py`）。
- `node + typescript.transpileModule`：通过（`dataset-source.ts`、`index.tsx`、`dataset-dropdown.tsx`、`dataset-card.tsx`）。
