# 2026-04-11 TK-010 Rework 01

## Rework Scope

This rework is limited to restoring the GraphRAG resume feature end-to-end.

The previous round fixed startup/import mismatch, but did not fully recover:

1. GraphRAG resume summary generation
2. GraphRAG resume/regenerate UI visibility in dataset settings

## Required Fixes

### 1. Repair Redis compatibility used by resume summary

Current runtime still fails in the resume summary path because the Redis wrapper does not fully support the hash operations expected by `task_monitor.py`.

You must make the current resume-summary implementation work with the real Redis abstraction used by this project.

This includes the code path behind:

- `GraphRAGTaskMonitor.get_doc_progress_all()`
- `GraphRAGTaskMonitor.get_counts()`
- `GraphRAGTaskMonitor.get_resumable_summary()`

### 2. Restore visible resume/regenerate UI

The project still contains GraphRAG resume/regenerate locale keys, but the active dataset settings UI currently does not expose the dedicated resume/regenerate flow.

You must restore the user-visible behavior in the dataset-settings GraphRAG entry so that:

- previous GraphRAG state can surface as resumable
- users can clearly see resume/regenerate actions again

## Files To Inspect

- `rag/graphrag/task_monitor.py`
- `rag/utils/redis_conn.py`
- `api/apps/kb_app.py`
- `web/src/components/parse-configuration/graph-rag-form-fields.tsx`
- `web/src/pages/dataset/dataset/generate-button/generate.tsx`
- `web/src/pages/dataset/dataset/generate-button/hook.ts`

## Acceptance Focus

`agent-x` will verify:

1. whether resume summary is actually available at runtime
2. whether GraphRAG resume/regenerate UI is visible again
3. whether the result restores the previous feature instead of only restoring startup

## Required Completion Record

After implementation, update or add:

- `project-work/agent-B/2026-04-10-TK-010-completion.md`

And set the work-order status back to:

- `pending acceptance`
