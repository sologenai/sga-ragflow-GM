# TK-006 Rework 02

## Basic Information

- Ticket ID: `TK-006`
- Rework Round: `02`
- Assignee: `agent-B`
- Requested By: `agent-x`
- Status: `open`
- Priority: `high`

## Rework Background

Rebuilt-image runtime verification on `ragflow-custom:tk006-tk007-r1` confirms that the first rework round made meaningful progress:

- `Thinking` no longer appears visually frozen after the first retrieval lines
- non-thinking + graph-enabled chat can now return visible graph evidence
- community-summary-first graph presentation is working

However, the stronger deep-retrieval path still fails at runtime during planned sub-query execution.

Reference review:

- `project-work/agent-x/2026-04-05-TK-006-review-round-2.md`

## Rework Scope

### 1. Fix deep-research sub-query execution in `Thinking` mode

This is required.

Current verified runtime failure:

- after the system plans next-step sub-queries, deep retrieval fails with:
  - `a coroutine was expected, got <_GatheringFuture pending>`

Primary file to inspect:

- `rag/advanced_rag/tree_structured_query_decomposition_retrieval.py`

Expected outcome:

- sub-query execution actually runs
- `Thinking` mode no longer falls back because of the awaitable/task mismatch
- deeper retrieval can continue past the planning stage and finish normally

### 2. Preserve the already-fixed progress continuity

Do not regress the first rework improvements.

Expected outcome:

- `Thinking` still emits visible progress after the initial retrieval lines
- success and failure paths still produce a clear stream ending

### 3. Preserve the already-fixed graph-evidence visibility

Do not regress the first rework improvements.

Expected outcome:

- non-thinking + graph-enabled chat still shows graph evidence
- community summary remains the strongest display path when available
- fallback behavior remains intact when no community summary exists

## In Scope

- fixing the deep-research awaitable/task mismatch
- minimal runtime correction for sub-query execution
- preserving current progress and graph-evidence improvements
- rework completion documentation

## Out Of Scope

- redesigning the whole deep-research architecture
- replacing `Thinking` semantics
- major chat UI redesign
- retrieval-strategy redesign beyond the specific runtime failure

## Acceptance Focus

`agent-x` will recheck:

1. whether `Thinking` reaches the planned sub-query stage without crashing
2. whether sub-query execution actually runs after planning
3. whether `Thinking` finishes without the `GatheringFuture` runtime failure
4. whether previously fixed progress continuity remains intact
5. whether previously fixed graph-evidence presentation remains intact

## Minimum Self-Test

Before handoff, verify and report:

1. `Thinking` reaches sub-query planning: pass / fail
2. `Thinking` runs planned sub-queries without awaitable/task runtime failure: pass / fail
3. `Thinking` returns a completed final stream: pass / fail
4. non-thinking + graph-enabled chat still returns visible graph evidence: pass / fail
5. community-summary-first presentation remains intact: pass / fail
6. no regression in document/image references: pass / fail

## Required Documentation Update

After rework, update:

1. `project-work/agent-B/2026-04-04-TK-006-work-order.md`
   - status should move to `pending acceptance`

2. add completion record:
   - `project-work/agent-B/2026-04-05-TK-006-rework-02-completion.md`
