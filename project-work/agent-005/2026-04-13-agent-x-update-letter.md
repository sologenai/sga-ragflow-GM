# 2026-04-13 Archive Sync Update Letter For agent-x

- From: `agent-005`
- To: `agent-x`
- Topic: `档案同步联调第二次更新`
- Status: `code updated / root cause narrowed further`

## 1. 本轮已完成的代码调整

1. 已把档案同步拆成两种模式：
   - `incremental`：按时间窗口筛选
   - `full`：全量拉取
2. 已明确约束：
   - 手动可选全量/增量
   - 定时任务固定走增量
3. 已保留全量去重：
   - 全量同步时仍按 `docid` 做去重
   - 已存在文档不会重复上传
4. 已修正档案 PDF 的解析方式：
   - 档案知识库默认解析器由 `presentation` 改为 `naive`
   - 上传成功后会强制把文档解析器设为 `naive`
   - 上传后会自动调用 `DocumentService.run(...)` 排队解析
5. 已补增量配置项：
   - `incremental_days`
   - 设置页已支持配置增量回溯天数

## 2. 当前对“综合行政事务只同步回 6 份”的进一步判断

这件事目前看不只是“增量窗口太小”。

从仓库里的接口样例 [`获取档案数据.md`](/E:/sga-ragflow-GM/获取档案数据.md:2) 可见：

1. `queryDataDetail` 返回的 `综合行政事务` 记录远不止 6 条
2. 但大量记录本身 `docid` 是空的
3. 我方下载附件时必须依赖：
   - `docid`
   - `tablename`
   - `tableid`
4. 没有 `docid` 的记录，当前无法继续调 `queryViewData` 取附件

按样例做的粗统计：

- `综合行政事务` 约 `34` 条
- 其中 `docid` 为空的约 `26` 条
- `docid` 非空的约 `8` 条

因此，“只同步回来 6 个文件”很可能不是我方单纯漏同步，而是档案侧返回的很多记录缺少附件查询所需主键，导致只能同步少数可下载附件的记录。

## 3. 当前最值得优先确认的外部问题

建议项目经理协调档案接口方尽快确认这两个问题：

1. `queryDataDetail` 返回的记录如果 `docid` 为空，是否还有别的字段可以用于查附件？
   - 例如：`docfid`、`docbarcode`、`docno`
   - 或者是否应改调另一个接口
2. `queryDataDetail` 是否存在默认分页/限条？
   - 当前文档样例中没有明确看到分页字段
   - 如果接口默认限条，我方还需要补分页循环

## 4. 下一步建议

下一步建议按优先级处理：

1. 先向档案侧确认“空 `docid` 记录如何取附件”
2. 同时确认 `queryDataDetail` 是否支持分页，以及分页参数名
3. 如果分页存在，我这边继续补分页抓全
4. 如果空 `docid` 记录有替代取附件方式，我继续补兼容逻辑

## 5. 备注

本轮 Python 相关改动已完成语法校验。  
运行时联调仍需在实际容器环境验证。
