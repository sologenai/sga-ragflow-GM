# RAGFlow 培训 PPT 详细生图提示词及构图设计 (厦门国贸 2026 定制版 - 全干货)

> **全局风格 (Global Style)**:
> 风格设定：高端商务科技风。深海蓝背景 (#0A192F) 搭配隐约的几何网格线条。
> 元素：3D 玻璃拟态，柔和的紫色 (#7030A0) 和电光青色光效。
> 规则：干净、极简、电影级 16:9 渲染。

---

## 第 1 页：封面：厦门国贸 RAGFlow 开发管理员培训
- **画面视觉构图**: 专业技术架构图。画面上方是大标题，画面下方是日期。画面中央展示完整的 RAGFlow 系统流程。
- **要求展示的文字及排版**: 上方："厦门国贸 RAGFlow 开发管理员培训"。下方："2026年3月"。中央：文档输入 → DeepDoc解析 → 切片 → Embedding → 向量库 → 检索 → LLM → 答案
- **最终中文生图 Prompt**:
```
专业技术架构图。画面上方：3D发光白色中文字"厦门国贸 RAGFlow 开发管理员培训"。画面下方：3D发光白色中文字"2026年3月"。画面中央：RAGFlow 系统架构框图，包含以下模块：文档输入 → DeepDoc解析 → 切片 → Embedding → 向量库 → 检索 → LLM → 答案。每个模块用中文标注。线条用青色和紫色表示数据流。
```

---

## 第 2 页：课程目标：从文档到智能问答
- **画面视觉构图**: 技术流程图，包含四个阶段，每个阶段配有图标。
- **要求展示的文字及排版**: 四个阶段：第一阶段-文档解析、第二阶段-混合检索、第三阶段-知识图谱、第四阶段-智能代理。每个阶段下方写相应目标。
- **最终中文生图 Prompt**:
```
技术流程图，包含四个阶段，每个阶段用中文标注：第一阶段：文档解析（DeepDoc），配图标文档和放大镜；第二阶段：混合检索（BM25+向量），配图标搜索；第三阶段：知识图谱（GraphRAG），配图标网络节点；第四阶段：智能代理（Agent），配图标机器人。每个阶段下方写中文：掌握解析模板、理解检索链路、配置知识图谱、实现业务自动化。
```

---

## 第 3 页：RAGFlow 核心架构
- **画面视觉构图**: 完整的系统架构图，分四层展示，用箭头连接。
- **要求展示的文字及排版**: 四层分别标注：应用层、检索层、解析层、文档层。每层列出包含的组件。
- **最终中文生图 Prompt**:
```
完整的系统架构图，分四层展示。最上层写中文：应用层，包含：对话、Agent、API、多终端。第二层写中文：检索层，包含：向量检索、关键词检索、图谱检索。第三层写中文：解析层，包含：DeepDoc、MinerU、Docling、布局识别、表格还原。最下层写中文：文档层，包含：PDF、Word、Excel、PPT、扫描件、网页。用青色和紫色箭头连接各层，形成数据流。
```

---

## 第 4 页：什么是 RAG：检索增强生成
- **画面视觉构图**: RAG 工作流程图，从左到右五个步骤。
- **要求展示的文字及排版**: 上方大标题：RAG = 检索 + 生成。五个步骤依次：用户问题、检索相关文档、作为上下文、生成答案、有据可查的答案。
- **最终中文生图 Prompt**:
```
RAG工作流程图，从左到右：第一步：用户问题，配图标问号，写中文"用户问题"；第二步：文档检索，配图标搜索，写中文"检索相关文档"；第三步：上下文拼接，配图标拼图，写中文"作为上下文"；第四步：LLM生成，配图标大脑，写中文"生成答案"；第五步：输出答案，配图标对勾，写中文"有据可查的答案"。上方大标题写中文：RAG = 检索 + 生成。
```

---

## 第 5 页：DeepDoc 核心：12种解析模板详解
- **画面视觉构图**: 解析模板对比图，5个并排卡片。
- **要求展示的文字及排版**: 五个卡片：Naive-通用模式-规章制度、Table-表格专家-财务报表、Laws-法律法规-合同条款、Manual-产品手册-操作指南、Paper-学术论文-技术文档。上方大标题：DeepDoc 12种解析模板。
- **最终中文生图 Prompt**:
```
解析模板对比图，5个并排的卡片。第一个卡片：Naive，写中文"通用模式-规章制度"；第二个卡片：Table，写中文"表格专家-财务报表"；第三个卡片：Laws，写中文"法律法规-合同条款"；第四个卡片：Manual，写中文"产品手册-操作指南"；第五个卡片：Paper，写中文"学术论文-技术文档"。每个卡片配相应图标。上方大标题写中文：DeepDoc 12种解析模板。
```

---

## 第 6 页：Table 模板实战：财务报表解析
- **画面视觉构图**: 表格解析流程图，左中右三部分。
- **要求展示的文字及排版**: 左侧：原始PDF表格。中间：DeepDoc Table模板解析。右侧：Markdown/HTML结构。下方配置参数：chunk_token_num=300, table_min_rows=2。
- **最终中文生图 Prompt**:
```
表格解析流程图，左侧：原始PDF表格（有合并单元格和跨页），写中文"原始PDF表格"；中间：解析过程，写中文"DeepDoc Table模板解析"，配图标齿轮；右侧：结构化输出，写中文"Markdown/HTML结构"，显示整齐的表格。下方写配置参数：chunk_token_num=300, table_min_rows=2。
```

---

## 第 7 页：Laws 模板：合同与法律法规
- **画面视觉构图**: 层级结构图，树状结构展示。
- **要求展示的文字及排版**: 根节点：合同文档。第一层：第一章 总则、第二章 采购、第三章 付款。第二层：第一条 定义、第二条 范围、第三条 供应商。第三层：（一）供应链、（二）货物、（三）服务。
- **最终中文生图 Prompt**:
```
层级结构图，树状结构。根节点写中文"合同文档"，第一层：第一章 总则、第二章 采购、第三章 付款；第二层：第一条 定义、第二条 范围、第三条 供应商；第三层：（一）供应链、（二）货物、（三）服务。用连接线连接，形成清晰的层级。配说明文字：Laws模板自动识别章-节-条-款。
```

---

## 第 8 页：切片参数配置：Chunk Size & Overlap
- **画面视觉构图**: 参数配置示意图，上下两部分。
- **要求展示的文字及排版**: 上方：Chunk Size滑块（300/512/1024），箭头指向512，标注：推荐 300-512 Tokens。下方：Overlap示意图，两个矩形重叠，标注10%-15%，写：Overlap 50-100 Tokens。
- **最终中文生图 Prompt**:
```
参数配置示意图，上方：Chunk Size滑块，从左到右标注：300、512、1024，箭头指向512，写中文"推荐：300-512 Tokens"。下方：Overlap示意图，两个矩形部分重叠，重叠区域高亮，标注10%-15%，写中文"Overlap：50-100 Tokens"。配说明：语义胶水，避免关键词被切断。
```

---

## 第 9 页：布局识别：智能去噪
- **画面视觉构图**: 文档去噪前后对比图，左右分屏。
- **要求展示的文字及排版**: 左侧：原始文档（标注页眉、页脚、水印、侧边栏，红色叉号），写：噪音内容。右侧：清理后的文档（绿色对勾），写：纯净正文。中间箭头：DeepDoc布局识别。配置参数：layout_recognize: true。
- **最终中文生图 Prompt**:
```
文档去噪前后对比图。左侧：原始文档，标注页眉、页脚、水印、侧边栏，用红色叉号标记，写中文"噪音内容"。右侧：清理后的文档，只保留正文，用绿色对勾标记，写中文"纯净正文"。中间箭头写中文"DeepDoc布局识别"。配置参数：layout_recognize: true。
```

---

## 第 10 页：多模态解析：OCR与图表理解
- **画面视觉构图**: 多模态解析流程图，左中右结构。
- **要求展示的文字及排版**: 左侧：输入（扫描合同、发票、技术图纸）。中间：OCR模块（PaddleOCR文字提取）、图表理解模块（多模态模型识别）。右侧：输出（结构化文字、图表描述）。配置：image2text_model=gpt-4-vision。
- **最终中文生图 Prompt**:
```
多模态解析流程图，左侧：输入（扫描合同、发票、技术图纸）配相应图标。中间：OCR模块，写中文"PaddleOCR文字提取"；图表理解模块，写中文"多模态模型识别"。右侧：输出（结构化文字、图表描述）。配置参数：image2text_model=gpt-4-vision。
```

---

## 第 11 页：混合检索：关键词 + 向量 + 重排
- **画面视觉构图**: 混合检索架构图，三个并行流汇聚。
- **要求展示的文字及排版**: 三个流：关键词检索（BM25精确匹配，权重0.3）、向量检索（Embedding语义匹配，权重0.7）、图谱检索（GraphRAG关系推理）。汇聚到Rerank模块（重排模型bge-reranker-large Top30→Top10）。最终输出：精准答案。
- **最终中文生图 Prompt**:
```
混合检索架构图，三个并行输入流：第一个流：关键词检索，写中文"BM25精确匹配"，权重0.3；第二个流：向量检索，写中文"Embedding语义匹配"，权重0.7；第三个流：图谱检索，写中文"GraphRAG关系推理"。三个流汇聚到Rerank模块，写中文"重排模型bge-reranker-large Top30→Top10"。最终输出：精准答案。
```

---

## 第 12 页：Embedding 模型选型：中文场景
- **画面视觉构图**: Embedding模型对比表，三个卡片并排。
- **要求展示的文字及排版**: 三个卡片：Qwen3-Embedding-VL（推荐首选-多模态适配-支持图像和文本，星级★★★★★）、Qwen3-Embedding（纯文本-速度更快，星级★★★★☆）、BGE-Large-zh-v1.5（中文效果稳定，星级★★★☆☆）。底部红色警告框：同一知识库必须使用相同Embedding模型，升级需重新向量化！
- **最终中文生图 Prompt**:
```
Embedding模型对比表，三个卡片并排。第一个卡片：Qwen3-Embedding-VL，写中文"推荐首选-多模态适配-支持图像和文本"，星级★★★★★；第二个卡片：Qwen3-Embedding，写中文"纯文本-速度更快"，星级★★★★☆；第三个卡片：BGE-Large-zh-v1.5，写中文"中文效果稳定"，星级★★★☆☆。底部红色警告框写中文：同一知识库必须使用相同Embedding模型，升级需重新向量化！
```

---

## 第 13 页：检索测试与调优
- **画面视觉构图**: 检索测试界面示意图，左中右三栏。
- **要求展示的文字及排版**: 左侧：测试问题输入框（测试问题：2025年采购政策是什么？）。中间：检索结果列表（Top-10结果，相似度分数）。右侧：评估指标面板（Top-5召回率92%、MRR 0.85、NDCG 0.88）。底部：目标 - Top-5召回率>90%。
- **最终中文生图 Prompt**:
```
检索测试界面示意图，左侧：测试问题输入框，写中文"测试问题：2025年采购政策是什么？"。中间：检索结果列表，显示Top-10结果，每个结果有相似度分数。右侧：评估指标面板，显示Top-5召回率92%、MRR 0.85、NDCG 0.88。底部写中文：目标 - Top-5召回率>90%。
```

---

## 第 14 页：知识图谱 GraphRAG：多跳推理
- **画面视觉构图**: 供应链知识图谱示意图，节点和边。
- **要求展示的文字及排版**: 节点：厦门国贸（蓝色）、供应商A（绿色）、供应商B（绿色）、客户C（橙色）、仓库D（紫色）。边标注：供应、采购、存储。说明文字：实体类型-组织/人员/地理位置；关系类型-供应/采购/隶属。下方：GraphRAG多跳推理：厦门国贸的供应商A的客户是谁？
- **最终中文生图 Prompt**:
```
供应链知识图谱示意图，节点包括：厦门国贸（蓝色）、供应商A（绿色）、供应商B（绿色）、客户C（橙色）、仓库D（紫色）。边标注：供应、采购、存储。配说明文字：实体类型-组织/人员/地理位置；关系类型-供应/采购/隶属。下方写中文：GraphRAG多跳推理：厦门国贸的供应商A的客户是谁？
```

---

## 第 15 页：GraphRAG 配置实战
- **画面视觉构图**: GraphRAG配置界面截图风格，左右两栏。
- **要求展示的文字及排版**: 左侧：配置表单（Enable GraphRAG已勾选、实体类型：组织,人员,地理位置,产品,合同、方法：Light/General选Light、实体消歧已勾选）。右侧：图谱预览（实体节点和关系连线）。底部：构建图谱需要时间和token，建议先在小数据集测试。
- **最终中文生图 Prompt**:
```
GraphRAG配置界面截图风格，左侧：配置表单，包含复选框Enable GraphRAG（已勾选）、实体类型输入框（组织,人员,地理位置,产品,合同）、方法选择（Light/General，选Light）、实体消歧（已勾选）。右侧：图谱预览，显示实体节点和关系连线。底部写中文：构建图谱需要时间和token，建议先在小数据集测试。
```

---

## 第 16 页：原文溯源：可信 AI
- **画面视觉构图**: 溯源界面示意图，上中下结构。
- **要求展示的文字及排版**: 上方：问题"2025年付款政策变更内容是什么？"。中间：LLM答案（蓝色高亮引用标记[1][2]）。下方：引用快照区域（[1] 2025年3月发布的《付款政策变更通知》原文片段；[2] 2025年4月财务部邮件原文片段）。说明文字：点击引用可跳转原文。
- **最终中文生图 Prompt**:
```
溯源界面示意图，上方：问题"2025年付款政策变更内容是什么？"。中间：LLM答案，用蓝色高亮标注引用标记[1][2]。下方：引用快照区域，显示两个片段：[1] 2025年3月发布的《付款政策变更通知》原文片段；[2] 2025年4月财务部邮件原文片段。配说明文字：点击引用可跳转原文。
```

---

## 第 17 页：Agent 工作流：从问答到自动化
- **画面视觉构图**: Agent工作流画布图，节点和箭头。
- **要求展示的文字及排版**: 工作流：Begin → Retrieval（检索知识库）→ Generate（LLM生成）→ Answer（输出答案）。旁边高级组件：Categorize分类、Switch分支、ExeSQL执行SQL、ExePython执行代码、HTTP请求。下方：预置模板：Simple RAG、Deep RAG、Self RAG、Agentic RAG。
- **最终中文生图 Prompt**:
```
Agent工作流画布图，节点用框和箭头连接。开始节点：Begin；然后是Retrieval（检索知识库）；然后是Generate（LLM生成）；然后是Answer（输出答案）。旁边列出高级组件：Categorize分类、Switch分支、ExeSQL执行SQL、ExePython执行代码、HTTP请求。下方写中文：预置模板：Simple RAG、Deep RAG、Self RAG、Agentic RAG。
```

---

## 第 18 页：System Prompt 工程：人设与约束
- **画面视觉构图**: System Prompt编辑器界面，左右两栏。
- **要求展示的文字及排版**: 左侧：Prompt编辑区域（示例Prompt："你是厦门国贸资深供应链风控专家。基于提供的知识库回答问题。规则：1. 只基于上下文回答；2. 不知道明确告知；3. 输出表格形式对比风险点。"）。右侧：提示词块下拉菜单（task_analysis、reflect、sufficiency_check）。下方：关键 - 明确人设、严格约束、规范输出。
- **最终中文生图 Prompt**:
```
System Prompt编辑器界面，左侧：Prompt编辑区域，显示示例Prompt："你是厦门国贸资深供应链风控专家。基于提供的知识库回答问题。规则：1. 只基于上下文回答；2. 不知道明确告知；3. 输出表格形式对比风险点。"。右侧：提示词块下拉菜单，显示：task_analysis、reflect、sufficiency_check。下方写中文：关键 - 明确人设、严格约束、规范输出。
```

---

## 第 19 页：核心参数：Temperature 配置
- **画面视觉构图**: Temperature仪表盘示意图，颜色渐变。
- **要求展示的文字及排版**: 从左到右：蓝色（0.1-0.3）→ 青色（0.3-0.6）→ 橙色（0.7-1.0）。指针指向0.2（蓝色区域）。蓝色区域：严谨准确-事实查询-推荐<0.3。青色区域：平衡-一般对话。橙色区域：发散创意-文案写作。底部红色框：厦门国贸场景 - Temperature < 0.3。
- **最终中文生图 Prompt**:
```
Temperature仪表盘示意图，从左到右颜色渐变：蓝色（0.1-0.3）→ 青色（0.3-0.6）→ 橙色（0.7-1.0）。指针指向0.2（蓝色区域）。每个区域标注：蓝色区域写中文"严谨准确-事实查询-推荐<0.3"；青色区域写中文"平衡-一般对话"；橙色区域写中文"发散创意-文案写作"。底部红色框写中文：厦门国贸场景 - Temperature < 0.3。
```

---

## 第 20 页：多业务知识库编排
- **画面视觉构图**: 知识库编排示意图，多个知识库框。
- **要求展示的文字及排版**: 知识库：合同库（蓝色）、政策库（绿色）、产品库（橙色）、财务库（紫色）、法务库（红色）。上方：检索请求（跨库检索）。下方：优先级设置（政策库1、合同库2、产品库3）。右侧：权限设置（Private仅自己、Team团队、Public公开）。
- **最终中文生图 Prompt**:
```
知识库编排示意图，多个知识库框：合同库（蓝色）、政策库（绿色）、产品库（橙色）、财务库（紫色）、法务库（红色）。上方：检索请求，箭头指向所有知识库，标注"跨库检索"。下方：优先级设置，政策库优先级1（最高）、合同库2、产品库3。右侧：权限设置，Private仅自己、Team团队、Public公开。
```

---

## 第 21 页：数据源同步：Confluence/S3/Notion
- **画面视觉构图**: 数据源同步架构图，左中右。
- **要求展示的文字及排版**: 左侧：外部数据源（Confluence、Amazon S3、Notion、Discord、Google Drive）。中间：RAGFlow同步引擎（自动同步-增量更新）。右侧：RAGFlow知识库。配置参数：同步频率-每天、检测变化-是、增量更新-是。说明文字：支持10+数据源，自动保持知识库最新。
- **最终中文生图 Prompt**:
```
数据源同步架构图，左侧：外部数据源图标：Confluence、Amazon S3、Notion、Discord、Google Drive。中间：RAGFlow同步引擎，写中文"自动同步-增量更新"。右侧：RAGFlow知识库。配置参数显示：同步频率-每天、检测变化-是、增量更新-是。配说明文字：支持10+数据源，自动保持知识库最新。
```

---

## 第 22 页：API 集成：业务系统对接
- **画面视觉构图**: API集成架构图，左中右。
- **要求展示的文字及排版**: 左侧：业务系统（OA系统、企业微信、钉钉、业务门户）。中间：API网关（HTTP API RESTful、Python SDK ragflow-sdk）。右侧：RAGFlow核心服务。下方代码示例：from ragflow_sdk import RAGFlow; client = RAGFlow(api_key='xxx'); kb = client.create_dataset(name='合同库'); kb.upload_document('contract.pdf')。
- **最终中文生图 Prompt**:
```
API集成架构图，左侧：业务系统图标：OA系统、企业微信、钉钉、业务门户。中间：API网关，显示两种方式：HTTP API（RESTful）、Python SDK（ragflow-sdk）。右侧：RAGFlow核心服务。下方代码示例：from ragflow_sdk import RAGFlow; client = RAGFlow(api_key='xxx'); kb = client.create_dataset(name='合同库'); kb.upload_document('contract.pdf')。
```

---

## 第 23 页：部署架构：Docker Compose
- **画面视觉构图**: Docker Compose部署架构图，容器图标。
- **要求展示的文字及排版**: 各服务：Web（React）、API（Flask/Quart）、MySQL（元数据）、Redis（缓存）、Elasticsearch/Infinity（文档引擎）、MinIO/S3（对象存储）。箭头连接数据流。底部资源要求：CPU≥4核，RAM≥16GB，Disk≥50GB，Docker≥24.0。
- **最终中文生图 Prompt**:
```
Docker Compose部署架构图，各服务用容器图标表示。前端：Web（React）；后端：API（Flask/Quart）；数据库：MySQL（元数据）；缓存：Redis；文档引擎：Elasticsearch/Infinity；对象存储：MinIO/S3。用箭头连接显示数据流。底部写资源要求：CPU≥4核，RAM≥16GB，Disk≥50GB，Docker≥24.0。
```

---

## 第 24 页：项目交付验收标准
- **画面视觉构图**: 验收标准仪表盘，四个指标卡片。
- **要求展示的文字及排版**: 四个卡片：准确率（Top-5召回率92%，MRR 0.85，绿色对勾）、响应速度（首字生成2.1秒，完整答案7.8秒，绿色对勾）、稳定性（7x24可用，故障率0.05%，绿色对勾）、用户满意度（4.7/5分，绿色对勾）。背景庆祝图标。
- **最终中文生图 Prompt**:
```
验收标准仪表盘，四个指标卡片。第一个卡片：准确率，显示Top-5召回率92%，MRR 0.85，绿色对勾。第二个卡片：响应速度，显示首字生成2.1秒，完整答案7.8秒，绿色对勾。第三个卡片：稳定性，显示7x24可用，故障率0.05%，绿色对勾。第四个卡片：用户满意度，显示4.7/5分，绿色对勾。背景是庆祝图标。
```

---

## 第 25 页：常见问题排障
- **画面视觉构图**: 排障指南表，四行两列。
- **要求展示的文字及排版**: 四行：第一行（问题-中文乱码，解决方案-检查编码UTF-8，启用PaddleOCR）；第二行（问题-表格识别差，解决方案-切换Table模板，table_min_rows=2）；第三行（问题-检索不相关，解决方案-调整Chunk Size/Overlap，降低阈值，启用Rerank）；第四行（问题-回答幻觉，解决方案-Temperature<0.3，加强Prompt约束）。每个解决方案旁绿色对勾。
- **最终中文生图 Prompt**:
```
排障指南表，四行两列。第一行：问题-中文乱码，解决方案-检查编码UTF-8，启用PaddleOCR。第二行：问题-表格识别差，解决方案-切换Table模板，table_min_rows=2。第三行：问题-检索不相关，解决方案-调整Chunk Size/Overlap，降低阈值，启用Rerank。第四行：问题-回答幻觉，解决方案-Temperature<0.3，加强Prompt约束。每个解决方案旁配绿色对勾图标。
```

---

## 第 26 页：最佳实践总结
- **画面视觉构图**: 最佳实践清单，四个要点。
- **要求展示的文字及排版**: 四个要点，每个大字体：第一点（解析策略：按文档类型选模板 - 合同Laws/报表Table/手册Manual）；第二点（切片参数：Chunk Size 300-512，Overlap 10%-15%）；第三点（检索配置：混合检索+Rerank，关键词0.3+向量0.7）；第四点（生成参数：Temperature<0.3，严格约束Prompt）。每个要点旁金色星星。
- **最终中文生图 Prompt**:
```
最佳实践清单，四个要点，每个用大字体标注。第一点：解析策略，写中文"按文档类型选模板 - 合同Laws/报表Table/手册Manual"。第二点：切片参数，写中文"Chunk Size 300-512，Overlap 10%-15%"。第三点：检索配置，写中文"混合检索+Rerank，关键词0.3+向量0.7"。第四点：生成参数，写中文"Temperature<0.3，严格约束Prompt"。每个要点旁配金色星星图标。
```

---

## 第 27 页：核心资产沉淀
- **画面视觉构图**: 知识资产沉淀示意图，从下到上四层。
- **要求展示的文字及排版**: 底层-原始文档（合同、政策、报表）；中层-结构化知识（解析、切片、向量化）；上层-智能应用（问答、Agent、图谱）；顶层-核心资产（企业知识库、知识图谱、最佳实践库）。向上箭头标注：持续进化、用户反馈、Bad Case优化。配文字：构建企业护城河。
- **最终中文生图 Prompt**:
```
知识资产沉淀示意图，从下到上：底层-原始文档（合同、政策、报表）；中层-结构化知识（解析、切片、向量化）；上层-智能应用（问答、Agent、图谱）；顶层-核心资产（企业知识库、知识图谱、最佳实践库）。用向上箭头连接，标注持续进化、用户反馈、Bad Case优化。配文字：构建企业护城河。
```

---

## 第 28 页：结语与Q&A
- **画面视觉构图**: 专业结语页，厦门国贸大厦剪影，日出背景。
- **要求展示的文字及排版**: 上方："感谢聆听"。下方："Q&A 问答环节"。底部："文档：docs.ragflow.io | GitHub：github.com/infiniflow/ragflow | 社区：Discord"。整体温馨专业。
- **最终中文生图 Prompt**:
```
专业结语页，画面中央：厦门国贸大厦轮廓剪影，日出背景。上方：3D发光白色中文字"感谢聆听"。下方：较小文字写"Q&A 问答环节"。底部：联系信息区域，写中文"文档：docs.ragflow.io | GitHub：github.com/infiniflow/ragflow | 社区：Discord"。整体风格温馨专业，象征合作与未来。
```

---

**注意**: 所有提示词均已优化为中文，明确要求生成的图像包含完整的中文文字、流程图、架构图、表格等内容，确保文字清晰准确无乱码。
